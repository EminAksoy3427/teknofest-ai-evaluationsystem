{
  "name": "@teknofest-ai/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Drizzle ve Cloudflare D1 kalıcılık sınırı.",
  "exports": {
    ".": "./src/index.ts",
    "./schema": "./src/schema/index.ts"
  },
  "scripts": {
    "auth:schema:generate": "auth generate --config=better-auth.config.ts --output=src/schema/auth.ts --yes",
    "db:generate": "drizzle-kit generate --config=drizzle.config.ts",
    "test": "node ./scripts/competition-member-schema.test.mjs",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@teknofest-ai/shared": "workspace:*",
    "drizzle-orm": "0.45.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260818.1",
    "auth": "1.7.0",
    "better-auth": "1.7.0",
    "drizzle-kit": "0.31.10",
    "typescript": "7.0.2"
  }
}
